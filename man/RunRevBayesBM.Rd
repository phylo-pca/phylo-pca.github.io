% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayesian.R
\name{RunRevBayesBM}
\alias{RunRevBayesBM}
\title{Bayesian inference of a tree from continuous character data}
\usage{
RunRevBayesBM(
  nexusFile,
  outputPrefix,
  rbPath = FindRevBayes(),
  maxHours = 2,
  seed = NULL
)
}
\arguments{
\item{nexusFile}{Path to a continuous-character Nexus file (see
\code{\link[=WriteContinuousNexus]{WriteContinuousNexus()}}).}

\item{outputPrefix}{File prefix for RevBayes' outputs (\verb{<prefix>.trees},
\verb{<prefix>.p.log}, \verb{<prefix>_map1.tre}, \verb{<prefix>_map2.tre}, ...).}

\item{rbPath}{Path to the RevBayes executable; see \code{\link[=FindRevBayes]{FindRevBayes()}}.}

\item{maxHours}{Wall-clock stopping rule, in hours.}

\item{seed}{Optional integer random seed, for a reproducible run (RevBayes
uses its own internal default seeding if omitted).}
}
\value{
A list with elements \code{mapTrees} (paths to the per-run maximum
\emph{a posteriori} tree files that were produced) and \code{log} (the RevBayes
console log file).
}
\description{
Runs the two-run, Metropolis-coupled Bayesian phylogenetic analysis of
continuous character data (a Brownian motion model, \code{dnPhyloBrownianREML})
used throughout Smith (2026), for both simulated continuous morphological
characters and Procrustes/RFTRA-aligned shape coordinates (both are
continuous data as far as the model is concerned). Each run stops itself
once the Gelman-Rubin PSRF falls below 1.01 and the parameter ESS exceeds
333 (or \code{maxHours} elapses).
}
