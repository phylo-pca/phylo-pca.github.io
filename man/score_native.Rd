% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{score_native}
\alias{score_native}
\title{Shape data maximum parsimony: native/Procrustes alignment}
\format{
A data frame with columns \code{idx} (job index), \code{t0} (tree index,
0-based), \code{dim}, \code{sigma}, \code{align} (\code{"proc"} or \code{"raw"}), \code{CID}, \code{RF},
\code{nmpt} (most-parsimonious trees found) and \code{ksel} (max-entropy
subsample size used).
}
\source{
\verb{hominin-pca-scale/R/24_shape_par_gen.R} +
\verb{R/26_shape_par_score.R}, run on Hamilton.
}
\usage{
score_native
}
\description{
Maximum-parsimony accuracy on shape (landmark) data (TNT's landmark data
type; the linear-displacement method of Goloboff & Catalano, following
Palci & Lee 2019; see \code{\link[=RunTNTParsimony]{RunTNTParsimony()}}), comparing the "native"
(TNT's own internal linear superimposition, no prior alignment) and
Procrustes-aligned (\code{\link[=ProcrustesAlign]{ProcrustesAlign()}}) treatments, across dimension and
\code{sigma}.
}
\note{
A re-run under the fuller unified search protocol (adding
\verb{lmark xthreads;} and a larger sectorial-search buffer; see
\code{\link[=RunTNTParsimony]{RunTNTParsimony()}}) was in progress on Hamilton at the time this
snapshot was taken. Prior validation showed the search was already at
the global optimum, so the re-run is expected to confirm rather than
change these values, but this file will be refreshed if it does.
}
\keyword{datasets}
