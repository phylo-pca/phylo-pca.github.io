% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trees.R
\name{TreeESS}
\alias{TreeESS}
\title{Topological effective sample size across RevBayes runs}
\usage{
TreeESS(runFiles, burnin = 0.25, cap = 1000, threshold = 200)
}
\arguments{
\item{runFiles}{Character vector of paths to RevBayes \code{.trees} files, one
per independent run.}

\item{burnin}{Proportion of samples discarded from each trace.}

\item{cap}{Maximum number of trees retained per run (traces longer than
this are evenly thinned before the ESS calculation, which scales poorly
with sample size).}

\item{threshold}{Minimum summed ESS (across runs) for convergence.}
}
\value{
A list with elements \code{ok} (logical, whether \code{threshold} is met),
\code{perRun} (a data frame of per-run ESS estimates), \code{summed} (the two
ESS statistics summed across runs) and \code{minEss} (the smaller of the two).
}
\description{
Estimates the effective sample size of tree topology across one or more
independent RevBayes runs, using the lower of the Frechet correlation and
median pseudo-ESS statistics (the pair used throughout Smith 2026), as
recommended by Fabreti & Hohna (2022) and Magee et al. (2024).
}
