% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trees.R
\name{RunRevBayesReinference}
\alias{RunRevBayesReinference}
\title{Re-infer a posterior of generative trees}
\usage{
RunRevBayesReinference(
  nexusFile,
  outDir,
  rbPath = FindRevBayes(),
  nGen = 250000,
  printGen = 50
)
}
\arguments{
\item{nexusFile}{Path to a discrete character Nexus file.}

\item{outDir}{Directory for RevBayes' logs and tree traces
(\code{reinfer.log}, \code{reinfer_run_1.trees}, \code{reinfer_run_2.trees}).}

\item{rbPath}{Path to the RevBayes executable; see \code{\link[=FindRevBayes]{FindRevBayes()}}.}

\item{nGen}{Number of MCMC generations per run.}

\item{printGen}{Sampling frequency, in generations.}
}
\value{
A list with elements \code{runFiles} (the two \code{.trees} files
produced, suitable input to \code{\link[=SampleStableTrees]{SampleStableTrees()}}) and \code{log}.
}
\description{
Runs the RevBayes re-inference used throughout Smith (2026) to obtain a
posterior of plausible generative trees for simulation: the Mk model
(Lewis 2001), matching Raskin et al. (2026)'s own model exactly except
for the ascertainment-bias correction (\code{coding = "variable"}, the
correct choice for a matrix with no invariant characters, such as
Mongle et al. (2023)'s; see \code{\link[=MongleStateProportions]{MongleStateProportions()}}). Two independent
runs are written so that topological convergence can subsequently be
assessed with \code{\link[=TreeESS]{TreeESS()}}. This step is CPU-intensive (hours, even for
21 taxa) - \code{generative_trees} provides the actual 250-tree sample used
in Smith (2026), sparing most users from re-running it.
}
