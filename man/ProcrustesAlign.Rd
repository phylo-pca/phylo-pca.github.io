% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/align.R
\name{ProcrustesAlign}
\alias{ProcrustesAlign}
\title{Generalized Procrustes alignment of landmark configurations}
\usage{
ProcrustesAlign(configs, tol = 1e-07, maxit = 200)
}
\arguments{
\item{configs}{A named list of landmarks x dim numeric matrices, one per
taxon (as returned by \code{\link[=ReadShapeConfigs]{ReadShapeConfigs()}}).}

\item{tol}{Convergence tolerance on the consensus configuration.}

\item{maxit}{Maximum number of GPA iterations.}
}
\value{
A named list of aligned landmarks x dim matrices, same names and
order as \code{configs}.
}
\description{
A minimal, dependency-free generalized Procrustes analysis (GPA):
centres each configuration on its centroid, scales it to unit centroid
size, then iteratively rotates every configuration (reflections
forbidden) onto the evolving consensus until convergence. Equivalent to
\code{geomorph::gpagen()} (Adams & Otarola-Castillo 2013) for landmark data
with no missing values or sliding semilandmarks, used throughout
Smith (2026) in preference to \code{geomorph::gpagen()} so that the exact
rotation/scaling steps are visible and independently checkable.
}
