% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trees.R
\name{SampleStableTrees}
\alias{SampleStableTrees}
\title{Sample a stable set of generative trees from a posterior}
\usage{
SampleStableTrees(
  runFiles,
  nTrees,
  stride = 11L,
  burnin = 0.25,
  threshold = 200,
  checkEss = TRUE
)
}
\arguments{
\item{runFiles}{Character vector of paths to RevBayes \code{.trees} files, one
per independent run.}

\item{nTrees}{Number of trees to sample.}

\item{stride}{Spacing (in interleaved-pool samples) between sampled trees;
should be odd, and large enough that consecutive draws are
effectively independent (checked via \code{\link[=TreeESS]{TreeESS()}} when \code{checkEss = TRUE}).}

\item{burnin}{Proportion of samples discarded from each trace.}

\item{threshold}{Minimum summed ESS (across runs) for convergence.}

\item{checkEss}{Whether to require the input traces to have converged
(summed topological ESS >= \code{threshold}) before sampling.}
}
\value{
A \code{multiPhylo} of \code{nTrees} sampled trees.
}
\description{
Draws a fixed, append-only set of "truth" trees from two independent
post-burnin RevBayes tree traces, for use as the generative tree in
simulation. Runs are interleaved (so both chains contribute from the
first tree onward) and then strided, so that \code{tree_k} is a fixed function
of \code{k}: raising \code{nTrees} later only appends new trees and never changes
the identity of an existing one.
}
