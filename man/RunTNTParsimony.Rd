% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parsimony.R
\name{RunTNTParsimony}
\alias{RunTNTParsimony}
\title{Maximum-parsimony tree search in TNT}
\usage{
RunTNTParsimony(
  data,
  type = c("continuous", "discrete", "landmark"),
  dim = NULL,
  tntPath = FindTNT(),
  workDir = tempfile("tnt"),
  timeout = 300,
  retrySeed = 7L
)
}
\arguments{
\item{data}{For \code{type = "continuous"}: a taxa x characters numeric
matrix. For \code{type = "discrete"}: a taxa x characters matrix of
single-character state labels. For \code{type = "landmark"}: a named list of
taxa's landmarks x \code{dim} numeric matrices (e.g. from \code{\link[=ProcrustesAlign]{ProcrustesAlign()}}
or \code{\link[=RftraAlign]{RftraAlign()}}) -- or, to search two or more shape characters
\emph{jointly} (e.g. a combined 2D+3D analysis), a list of such lists, one
per character, with \code{dim} given as a matching vector.}

\item{type}{Data type to search under; see \code{data}.}

\item{dim}{Number of spatial dimensions (required, and only used, when
\code{type = "landmark"}); a vector when \code{data} holds multiple characters.}

\item{tntPath}{Path to the TNT executable; see \code{\link[=FindTNT]{FindTNT()}}.}

\item{workDir}{Working directory for the (purely alphabetic, per TNT's
filename-parsing quirk) job script and tree file; created if absent.}

\item{timeout}{Maximum seconds to allow the search to run before
aborting (TNT occasionally hangs on a malformed/overflowing buffer).}

\item{retrySeed}{A second \code{rseed} to retry with, once, if the first
attempt fails or times out.}
}
\value{
A list with elements \code{trees} (a \code{multiPhylo} of every
most-parsimonious tree found, or \code{NULL} on failure) and \code{nTree} (the
number of most-parsimonious trees).
}
\description{
Runs a maximum-parsimony search in TNT (Goloboff et al. 2008; Goloboff &
Catalano 2016) using the unified search protocol validated in Smith
(2026) for continuous (Wagner), discrete (Fitch) and geometric-
morphometric landmark (Goloboff-Catalano linear displacement, following
Palci & Lee 2019) data, and returns every most-parsimonious tree found.
}
